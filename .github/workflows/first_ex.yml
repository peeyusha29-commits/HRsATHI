name: My  workflow

on: [push, fork]

jobs:
    _job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome message
             run: echo "My  Github Actions Job"

            - name: list files
                run: ls
                
            - name: Read Files    
                run: cat README.md

